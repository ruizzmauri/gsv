{
  "name": "gsv-docs",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "deploy": "vitepress build && wrangler pages deploy .vitepress/dist --project-name=gsv-docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4",
    "wrangler": "^4"
  }
}
